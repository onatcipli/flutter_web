name: Flutter Web
on:
  push:
    branches:
      - master
jobs:
  build:
    name: Build Web
    env:
      my_secret: ${{secrets.commit_secret}}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: subosito/flutter-action@v1
        with:
          channel: 'dev'
      - run: flutter config --enable-web
      - run: flutter pub get
      - run: flutter build web --release
      - run: |
          cd build/web
          git init
          git status
          git remote add origin https://${{secrets.commit_secret}}@github.com/onatcipli/onatcipli.github.io.git
          git checkout -b gh-pages
          ls
          git add --all
          ls
          git commit -m "update"
          ls
          git push origin gh-pages -f
